# Debian Linux 11: Instrucciones de instalación específicas para paquete binario (.DEB)

Si deseas configurar una nueva **máquina virtual** utilizando **Debian 11** para alojar tu sonda de RIPE Atlas, sigue los pasos a continuación.

---

## 1. Configurar Debian 11

1. Realiza una instalación limpia de **Debian 11** (por ejemplo, usando **VirtualBox** o **Parallels**).  
   Consulta la guía oficial de instalación de Debian 11 si es necesario.

2. Durante la instalación en la máquina virtual, **configura el adaptador de red virtual en modo “bridge”** (puente).  
   Esto es importante para permitir que funcione **IPv6**.  
   El modo predeterminado a menudo se llama *“shared”* o *“NAT”*, y solo proporciona conectividad IPv4.

---

## 2. Paquetes binarios mantenidos por RIPE NCC

RIPE NCC mantiene paquetes binarios **.DEB** actualmente disponibles para **Debian 11 (x86_64)**.  
Estos paquetes han sido verificados y funcionan correctamente en **Debian 11 (Bullseye)**.

---

## 3. Agregar el repositorio e instalar el paquete

Sigue los pasos para agregar el repositorio e instalar la sonda:

### 🔹 Paso 1: Descargar el paquete DEB del repositorio

```bash
curl -O 'https://ftp.ripe.net/ripe/atlas/software-probe/debian/dists/bullseye/main/binary-amd64/ripe-atlas-repo_1.5-5_all.deb'
```

### 🔹 Paso 2: Verificar el hash SHA256 del paquete

```bash
sha256sum -b ripe-atlas-repo_1.5-5_all.deb
```

El resultado **debe coincidir con el siguiente valor**:

```
8105140585dc4e1bf5b893832f1d4b6e7c2671b55b578bb792c305996df266a2
```

### 🔹 Paso 3: Instalar el paquete del repositorio

Ejecuta con privilegios de **root** o mediante **sudo**:

```bash
sudo dpkg -i ripe-atlas-repo_1.5-5_all.deb
```

### 🔹 Paso 4: Instalar la sonda de software

Actualiza los repositorios e instala el paquete principal:

```bash
sudo apt-get update
sudo apt-get install ripe-atlas-probe
```

Cuando el sistema te lo solicite, responde **“y”** para confirmar la instalación.

---

## 4. Registro y activación de la sonda

Al finalizar la instalación, el sistema mostrará instrucciones en pantalla para **registrar y activar la sonda**.  
Este paso es obligatorio para que la sonda funcione correctamente.

El proceso de registro requiere la **clave pública** de la sonda, que se encuentra en:

```
/etc/ripe-atlas/probe_key.pub
```

Copia el contenido de ese archivo y utilízalo en el formulario de registro disponible en:  
 [https://atlas.ripe.net/apply/swprobe/](https://atlas.ripe.net/apply/swprobe/)

---

**© RIPE NCC – Traducción técnica no oficial al español.**
